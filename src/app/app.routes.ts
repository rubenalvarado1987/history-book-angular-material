import { Routes } from '@angular/router';
import { HistoryBookComponent } from './components/component-library/component-library.component';

export const routes: Routes = [
  { path: '', component: HistoryBookComponent },
];